import * as React from 'react';
import * as ReactDOM from 'react-dom';

import ContainerComponent from './components/Container';

ReactDOM.render(<ContainerComponent />, document.getElementById('contentContainer'));