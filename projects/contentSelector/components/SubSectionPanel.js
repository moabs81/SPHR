import React, { Component } from 'react';
import PropTypes from 'prop-types';

export const SubSectionPanel = ({ SubSection }) => {
    return (
        <div className='SubSectionRow'>{SubSection}</div>
    );
};